/*
* File name: recursive.hpp
* Description: Holds prototypes for recursive.cpp
*/

#ifndef RECURSIVE_HPP
#define RECURSIVE_HPP

#include <cstring>
#include <string>
using std::string;

string reverse_str(string str);
int sum(int* list, int size);
int triangulate(int n);

#endif