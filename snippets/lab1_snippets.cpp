    //matrix calculator 2x2
    
    if(size == 2)
    {
        /*first = arr[x][y]; //0,0
        cout << "First value: " << first << endl;
        cout << "x: " << x << " y: " << y << endl;
        for(int i = 0; i < size-1; i++)
        {
            if(x == 0 && y == 0)    //for first diagonal pair
            {
                x++;
                y++;
                second = arr[x][y]; //1,1
                cout << "First value: " << first << endl;
                cout << "Second value: " << second << endl;
                first_product = first*second;   //(0,0) * (1,1)
            }
            
            if(x != 0 && y != 0) 
            {
                x--;
                cout << "x: " << x << " y: " << y << endl;
                first = arr[x][y];  //0,1
                x++;
                cout << "x: " << x << " y: " << y << endl;
                y--;
                cout << "x: " << x << " y: " << y << endl;
                second = arr[x][y]; //1,0
                cout << "First value: " << first << endl;
                cout << "Second value: " << second << endl;
                second_product = first*second;
            }
            cout << "First pair product: " << first_product << endl;
            cout << "Second pair product: " << second_product << endl;
            first_product -= second_product;    //replace as determinant
        }*/